name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint -w apps/docs

      - name: Type check
        run: npm run typecheck -w apps/docs

      - name: Build registry
        run: npm run registry:build -w apps/docs

      - name: Build app
        run: npm run build -w apps/docs
